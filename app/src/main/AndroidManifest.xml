<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pstlservices.offers24">

    <application
        android:name=".application.SellerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme">
        <activity android:name=".ui.product.activity.AddProductActivity"></activity>
        <activity android:name=".ui.orders.activity.OrderDetailActivity" />
        <activity android:name=".ui.orders.activity.AddOfferActivity" />
        <activity android:name=".ui.edit.EditBusinessActivity" />
        <activity android:name=".ui.otp.OtpActivity" />
        <activity android:name=".ui.SignupActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>